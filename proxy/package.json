{
  "name": "juslegal-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for JusLegal API calls",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
